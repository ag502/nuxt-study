{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "export SET NODE_OPTIONS=--openssl-legacy-provider && json-server --port 8000 --watch db.json",
    "generate": "json-server generate.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.16.3",
    "lodash": "^4.17.21"
  }
}
